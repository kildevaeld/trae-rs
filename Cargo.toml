[package]
name = "trae"
version = "0.1.0"
edition = "2024"

[dependencies]
slotmap = { version = "1", default-features = false }

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "tree"
harness = false
